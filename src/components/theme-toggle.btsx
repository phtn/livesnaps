import { Icon } from '@/lib/icons'
import { useTheme } from './theme-provider.btsx'

setup
  const { theme, toggleTheme } = useTheme()
  const nextTheme = theme === 'dark' ? 'light' : 'dark'
  const title = `Switch to ${nextTheme} theme`

button(
  ~ type='button'
  ~ className='inline-flex size-9 cursor-pointer items-center justify-center rounded-full border border-black/10 bg-black/5 text-foreground transition-colors hover:bg-black/10 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-foreground/30 dark:border-white/15 dark:bg-white/10 dark:hover:bg-white/15'
  ~ onClick={toggleTheme}
  ~ aria-label='Dark theme'
  ~ aria-pressed={theme === 'dark'}
  ~ title={title}
  ~ )
  Icon(name='theme' className='m-0 size-4')
