
import { cn } from "@/lib/utils";
import type { ComponentProps } from "octane";
component SkeletonRoot
  props { className, ...props }: ComponentProps<"div">
  div(data-slot="skeleton" className={cn("bg-sidebar animate-pulse", className)} {...props})

props {...props}: ComponentProps<"div">
SkeletonRoot({...props})
